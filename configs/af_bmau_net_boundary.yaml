# AF-BMAU-Net trained with boundary loss, batch size 8, 25 epochs.
# Only fields that differ from configs/default.yaml need to be listed.
name: af_bmau_net_boundary

models_to_run:
  - AF_BMAU_Net

loss_name: boundary
batch_size: 8
epochs: 25
