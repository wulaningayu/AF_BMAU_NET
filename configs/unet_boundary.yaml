# U-Net trained with boundary loss, batch size 8, 25 epochs.
# Only fields that differ from configs/default.yaml need to be listed.
name: unet_boundary

models_to_run:
  - UNet

loss_name: boundary
batch_size: 8
epochs: 25
