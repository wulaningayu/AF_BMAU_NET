# U-Net trained with hybrid loss, batch size 8, 25 epochs.
# Only fields that differ from configs/default.yaml need to be listed.
name: unet_hybrid

models_to_run:
  - UNet

loss_name: hybrid
batch_size: 8
epochs: 25
