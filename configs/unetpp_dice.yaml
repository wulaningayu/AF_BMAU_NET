# U-Net++ trained with dice loss, batch size 8, 25 epochs.
# Only fields that differ from configs/default.yaml need to be listed.
name: unetpp_dice

models_to_run:
  - UNetPP

loss_name: dice
batch_size: 8
epochs: 25
